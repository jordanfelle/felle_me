{
  "name": "felle-site",
  "version": "1.0.0",
  "description": "felle.me static site",
  "scripts": {
    "build": "bash ../build.sh",
    "dev": "bash ../build.sh && cd .. && hugo server --disableFastRender"
  }
}
